import SwiftUI

/// Not implemented — see this module's README for scope and status.
struct IndoorNavigationView: View {
    var body: some View {
        ContentUnavailableView(
            "Indoor Navigation",
            systemImage: "antenna.radiowaves.left.and.right",
            description: Text("Not yet implemented — see Features/IndoorNavigation/README.md")
        )
    }
}
