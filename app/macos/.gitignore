# Flutter-related
**/Flutter/ephemeral/
**/Pods/

# macOS build isn't used yet; ignore CocoaPods files regenerated by stray
# `flutter pub get` runs so they don't show up as untracked noise.
Podfile
Podfile.lock

# Xcode-related
**/dgph
**/xcuserdata/
